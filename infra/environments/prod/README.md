# Production gate

No production stack is instantiated yet. After church acceptance, create the separate account and configure distinct expected account ID, state, roles, domain, and release approvals. Refactor the validated development foundation into environment-parameterized modules at that stage; never point the dev root at production or reuse development state. Production configuration must reject the development account ID. This file is not a production Terraform deployment.
